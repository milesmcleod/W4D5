<h3>notes</h3>
<ul>
<% track.notes.each do |note| %>
  <li>
    <h3>from: <%= note.user.email %></h3>
    <h4><%= note.body %></h4>
    <% if note.user == current_user %>
      <%= button_to "Delete Note", note_url(note.id), method: :delete %>
    <% end %>
  </li>
<% end %>
</ul>
